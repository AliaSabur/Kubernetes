
教程说明:

写文档并用本地虚拟机进行视频演示部署过程，用`k8s`搭建`集群数据库`和`web`程序。

### **下面的教程中，软件版本都用新版即可。**

- 在编写文档和视频演示的时候，使用`OpenSSL`生成自签名证书，使用`example.com`作为演示域名，可以使用多个演示域名，例如`ssl.example.com`、`www.example.com`等等。

- 在演示的时候，通过修改本机的`host`文件实现域名与`ip`的绑定即可。

- 1台作为`master`节点，2台作为`node`节点。

- 演示中包含环境安装和部署部分，确保配置好的环境在系统重启后，依然生效。

---

服务器系统是全新的`Debian 12`，用1台做`master`节点，2台做`node`节点。`搭建集群数据库`和`web`程序。

使用`KubeSphere`作为`k8s`的管理面板，便于管理和维护。

在`KubeSphere`安装部署`Nginx`或者`OpenResty`，便于通过域名访问对应服务，里面的网站证书使用`OpenSSL`生成即可，支持通过`https`访问。

### **集群数据库需求如下：**

用`KubeSphere`部署`MariaDB`集群数据库、`PostgreSQL`集群数据库、`Redis`集群数据库，持久化数据库中的数据。实现

1. **同步复制**：所有节点的数据实时同步，确保数据的一致性。

2. **多主架构**：任何节点都可以处理读写请求，提供高可用性和灵活性。
(如果不支持多主架构，使用主从架构也行)

3. **自动节点管理**：
   - **自动成员管理**：节点可以动态加入或退出集群。
   - **自动故障转移**：当节点发生故障时，集群会自动重新配置，确保服务的连续性。

4. **无单点故障**：通过多节点冗余，避免单点故障影响整个集群。

5. **定时备份**

### **在某一台node节点上部署数据库管理工具，用于管理不同的数据库，支持增，删，改，查**

1. 在`KubeSphere`上部署`PostgreSQL`数据库管理工具`pgAdmin 4`，关联域名`pgAdmin.example.com`(或者直接通过`http://ip:端口/`访问也行)。

2. 在`KubeSphere`上部署`MariaDB`数据库管理工具`phpMyAdmin`，关联域名`phpMyAdmin.example.com`(或者直接通过`http://ip:端口/`访问也行)。

3. 在`KubeSphere`上部署`Redis`数据库管理工具`redis-commander`，关联域名`redis.example.com`(或者直接通过`http://ip:端口/`访问也行)。

4. 分别用这些数据库管理工具连接并读写一下对应的数据库，测试是否部署成功，同时也便于后续维护。


在部署好上面的数据库后，再部署`WordPress`应用，关联到域名`www.example.com`。其中`WordPress`使用`MariaDB`作为数据库。实现负载均衡和自动故障转移，配置好网站证书，支持通过`http`和`https`访问网站。

---
